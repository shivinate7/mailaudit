{
  "name": "mail-day-ledger",
  "version": "1.0.0",
  "private": true,
  "description": "TCGplayer order check-in tracker fed by OrderWand CSV exports",
  "scripts": {
    "build": "node build.mjs",
    "test": "node test/app.test.mjs",
    "serve": "node dev-server.mjs",
    "deploy": "npm run build && npm test && git add index.html && git commit -m \"deploy: rebuild index.html\" && git push"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "jsdom": "^29.1.1"
  },
  "dependencies": {
    "papaparse": "^5.4.1",
    "react": "^18.3.0",
    "react-dom": "^18.3.0"
  }
}
