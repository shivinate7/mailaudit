{
  "name": "mail-day-ledger",
  "version": "1.0.0",
  "private": true,
  "description": "TCGplayer order check-in tracker fed by OrderWand CSV exports",
  "scripts": {
    "build": "node build.mjs",
    "deploy": "npm run build && git add index.html && git commit -m \"deploy: rebuild index.html\" && git push"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  },
  "dependencies": {
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "papaparse": "^5.4.1"
  }
}
