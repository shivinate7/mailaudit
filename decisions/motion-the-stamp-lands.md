# The RECEIVED stamp lands, it does not appear

The stamp rocks down and settles at its resting angle instead of blinking into place. The order-stamp band shares the same keyframes through a `--rest` custom property.

## The argument, as recorded

```
- **The RECEIVED stamp lands** rather than blinking into being: down at −13°,
  rocked flat, settled back to its −4°. The order-stamp band uses the same
  keyframes and returns to its own −3° — the resting angle rides in a `--rest`
  custom property, so one animation serves both.
```

## See also

- `motion-timings` — the exact angles and duration this landing uses.
- `motion-is-stationery-not-software` — the register this landing is built to fit.
