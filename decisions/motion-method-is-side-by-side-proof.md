# Each motion was chosen by looking, not by reading

Every moment in the motion design was built two or three ways as a side-by-side proof sheet, holding every variable but one constant. The winning version was picked by looking at it, not by reading a description of it.

## The argument, as recorded

```
`_studies.html` is not in the repo, but the method that produced this is worth
keeping: each moment was built two or three ways as a side-by-side proof sheet
with every variable but one held constant, and picked by looking rather than by
reading a description.
```

## See also

- `motion-remeasure-not-rereason` — why these choices need a real browser to re-check.
- `motion-is-stationery-not-software` — the register these proof sheets were built to hit.
