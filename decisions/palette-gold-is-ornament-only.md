# Gold is ornament only, never text

The `gold` token fails the contrast floor for text by a wide margin. The palette-contrast record has the measured ratio. It exists only for the masthead's ornamental rules.

## The argument, as recorded

```
- **`gold`** is ornament only. At 2.2:1 on `card` it fails for text by a wide
  margin; it exists for the masthead rules and nothing else.
```

## See also

- `palette-contrast` — the measured ratio behind this ruling.
- `palette-token-table-and-contrast-floor` — the full token table this token belongs to.
